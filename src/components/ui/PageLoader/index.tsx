import CircularProgress from '../CircularProgress';
import * as s from './styles';

const PageLoader = () => {
  return (
    <s.Container>
      <CircularProgress />
    </s.Container>
  );
};

export default PageLoader;
