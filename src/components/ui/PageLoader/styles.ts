import { Box, styled } from '@mui/material';

const Container = styled(Box)`
  display: flex;
  flex: 1;
  min-height: 100vh;
  justify-content: center;
  align-items: center;
`;

export { Container };
